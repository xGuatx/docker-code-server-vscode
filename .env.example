# Code Server Configuration
# Change these passwords before deploying!

# Main password for web interface
CODE_SERVER_PASSWORD=your_secure_password_here

# Optional: Hashed password (leave empty if using PASSWORD)
CODE_SERVER_HASHED_PASSWORD=

# Sudo password for terminal
CODE_SERVER_SUDO_PASSWORD=your_secure_sudo_password_here

# Optional: Hashed sudo password (leave empty if using SUDO_PASSWORD)
CODE_SERVER_SUDO_PASSWORD_HASH=

# Optional: Custom domain for proxy
CODE_SERVER_PROXY_DOMAIN=code-server.my.domain
